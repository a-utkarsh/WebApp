$(document).ready(function(){
	init();
})

function init () {
	$('#login_submit').on ("click", function (ev) {
		var email = $ 
		var password = 

			var post_data = {
				username : email,
				password : password
			}

	})
}
